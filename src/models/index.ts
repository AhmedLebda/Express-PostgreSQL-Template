import User from "./user";

//*** Define Relations between tables here

// Example.belongsTo(User);
// User.hasMany(Example);

// User.belongsToMany(ExampleTwo, { through: UserExampleTwo, as: "example-title" });
// ExampleTwo.belongsToMany(User, { through: UserExampleTwo, as: "example-title" });

export { User };
